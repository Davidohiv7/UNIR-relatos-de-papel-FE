#!/bin/sh
# Hook de Husky que se ejecuta antes de cada commit.
# Carga la configuración necesaria de Husky y luego ejecuta lint-staged
# para validar y formatear los archivos en staging.
. "$(dirname "$0")/_/husky.sh"

npx lint-staged 